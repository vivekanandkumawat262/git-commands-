✅First step  : Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
It temporarily allows PowerShell to run scripts in the current terminal session only.

✅Second step : git init



✅Third step : git add .

✅Fourth step : git commit -m "added readme.md"


✅Fifth step : git remote add origin https://github.com/vivekanandkumawat262/git-commands-.git
✅Sixth step : git branch -M main
✅Seventh step : git push -u origin main

🔹 Run THIS command in PowerShell:
 This safely deletes the .git folder from backend.
✅Remove-Item -Recurse -Force .git

